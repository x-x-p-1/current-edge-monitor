# 项目任务清单（TODO / Roadmap）

> 本文件是**主任务跟踪器**，所有主线工作集中在这里维护，团队通过 GitHub 共同更新。
> 图例：`- [ ]` 待办 · `- [x]` 完成 · 优先级 `P0` 关键路径/阻塞 · `P1` 重要 · `P2` 增强
> 配套：《08_鲁棒性/鲁棒性清单.md》是**连续不停机运行**的鲁棒性专项要求与验收门禁。

## 状态总览

- [x] M0 软件侧全部完成（仿真/预处理/特征/模型/后处理/部署，63/63 测试）
- [x] 实时看板（连续循环播放）+ M0 仿真报告
- [x] GitHub 仓库建立、初版推送、`07_测试工具` 整理
- [x] **v2.1 预处理去直流修复**：慢基线(一阶高通)替代小窗滑动均值，基波保留 15%→100%；
      流式管线重构为「状态延续滤波 + 切帧」，消除重叠帧导致的状态错位（新增 6 回归用例）
- [x] **M1 采集层核心（09_采集层，21 用例）**：全速率环形缓冲 / 毫秒级看门狗特征 /
      触发引擎（EWMA 基线 + K·σ + 去抖 + 迟滞复位）/ 预+后触发切片落盘 / AcquisitionEngine；
      仿真端到端验证（堵转→触发→切片）每帧 ~0.09ms（目标 1.8ms）
- [ ] **硬件/数据线 = 当前瓶颈（关键路径）**
- [ ] 仿真器改造（00 模块）：需求评审稿已出，等电气工程师确认 → `00_数据生成与仿真/仿真器改造问题清单.md`

---

## G. 仿真器改造（00 模块，P1）

> 需求评审稿：`00_数据生成与仿真/仿真器改造问题清单.md`（六大方向：场景构建器 /
> 真实 VFD 波形 / 新故障类型 / 数据契约对齐 / 流式生成 / 混沌注入）
> 状态：**评审中**，等电气工程师逐条确认后再编码。

- [x] 5kW 电机物理模式：stall / load_step / unbalance 真实安培化（Motor 模型，06 回归 57/57）
- [x] 5kW 动态看板：`sim/dashboard_5kw.html`（播放/调速/拖动，真实安培读数）
- [x] **预处理去直流窗 bug**（v2.1 已修复）：
      `remove_dc_offset(window=100)` @16k 把 50Hz 基波砍掉 ~85%
      → 默认改用慢基线（一阶高通 fc=0.5Hz），基波保留 100%；
        流式管线重构为「状态延续因果滤波（去DC+带通）→ 切帧」，
        消除重叠帧（stride<window）导致滤波状态回退错位的问题。
        详见 `01_信号预处理/README.md` §v2.1 变更与 `06_测试与验证/test_preprocess.py` TestDCBaselineV21
- [ ] 电气工程师评审问题清单（汇总决策表 §7）
- [ ] 数据契约对齐：8 参数契约完整落地 metadata + 契约 JSON（P0）
- [ ] 故障库扩充：缺相 / 轴承 / 定子匝间短路（P0，联动鲁棒性 R6）
- [ ] 场景化构建器：Scenario DSL + ground-truth 事件表（P0）
- [ ] 真实 VFD 波形：频率斜坡 / SVPWM / 死区 / 启动涌流（P1）
- [ ] 流式/连续生成：CurrentStreamSimulator + soak 循环（P1）
- [ ] 混沌注入：ChaosInjector 对齐 08 鲁棒性 4.2（P1）
- [ ] 兼容性回归：verify_simulator / M0 / dashboard / 06 pytest 全绿（P0）

## A. 硬件与数据线（P0，关键路径）

- [ ] 三份需求清单发给电气 / 数学家 / 采集侧负责人（`边缘电流采集器/` 下）
- [ ] 回收反馈，锁目标设备画像（变频电机：型号 / 额定 / 供电 / 传感器 / 负载工况）
- [ ] 确认数据契约 8 参数（采样率 / 位深 / 标定 / 满量程削波 / 时戳连续性 / 帧 CRC / 噪声底）
- [ ] 板卡选型对比：RK3588S（开发）→ A733（量产）性能余量验证
- [ ] 邀请同事加入 GitHub 仓库（Settings → Collaborators → Add people）
- [ ] 第一批真实数据采集（至少 1 段正常 + 1 段异常/故障记录）

## B. M1 采集层（P0，软件主线）

- [x] 全速率环形缓冲（固定容量，常开）→ `09_采集层/ring_buffer.py`
- [x] 毫秒级看门狗特征（RMS / 包络 / 斜率，实测 ~0.09ms/帧 ≪ 1.8ms）→ `09_采集层/watchdog.py`
- [x] 触发引擎（stopping time：EWMA 基线 + K·σ 统计判据 + 去抖确认 + 迟滞复位；
      ARL 数学保证归 E·P1）→ `09_采集层/trigger.py`
- [x] 切片捕获：预触发 + 后触发落盘，携带上下文（时戳 + 原因 + meta）→ `09_采集层/slice_capture.py`
- [x] AcquisitionEngine 端到端（feed 驱动主循环）→ `09_采集层/engine.py` + `07_测试工具/verify_acquisition.py`
- [ ] 特征日志常存 + 原始默认释放（数据飞轮 = 未来 ML 训练数据）
- [ ] 慢路径按过程状态门控（状态机 → 慢路径调度，M0 报告优化项）

## C. 鲁棒性加固（P0 —— 连续不停机运行）

> 原则：**算法必须在被监测设备坏之前不能坏。**
> 明细、防护与验收标准见《08_鲁棒性/鲁棒性清单.md》。

- [ ] 数值稳健：全链路 NaN/Inf 防护（断线 / 除零 / 高增益滤波）
- [ ] 全局异常捕获：任何单帧异常不杀进程，记录并跳过
- [ ] 内存有界：特征日志 / 事件聚合 / 看门狗队列无界增长检查
- [ ] 状态机防漂移：自适应基线（遗忘因子，仅稳态更新）+ 基线重锚 + 卡死检测
- [ ] 异常输入标记：ADC 削波 / 直流偏置 / 断线 / 缺相 / 相位错序 / 丢样
- [ ] 看门狗自检：算法心跳 + 输入质量自评 + 优雅降级链
- [ ] 故障恢复：事件后状态机复位路径（迟滞 + 驻留超时）
- [ ] 持久化：基线 / 标定 checkpoint，断电重启恢复
- [ ] 24h / 72h 长时间压力测试（soak）：内存曲线有界、无 NaN、状态不卡死
- [ ] 混沌注入测试：NaN / 断线 / 饱和 / 偏置 / 丢样 / 异常注入 → 不崩溃、可标记、可恢复

## D. 测试与验证增强（P1）

- [ ] 数据体检 QA 脚本（第一批真实数据：契约校验 + 质量画像）
- [ ] 性能基线基准（RK3588 / A733 每帧 CPU p99、内存峰值）
- [ ] 回归套件扩充：鲁棒性用例并入 pytest 门禁

## E. 数学家 / 研究轨道（P1，与软件并行）

- [ ] P1 触发统计：ARL / 误报率的数学保证
- [ ] P2 特征规范：命名 / 维度 / 数值域 / 可解释性
- [ ] P3 无监督正常建模：AE / 马氏，接管自适应基线（终极形态）
- [ ] P4 差异化：变转速鲁棒性、跨机域适应、弱信号提取

## F. 部署与量产（P2）

- [ ] ONNX 导出验证 → RKNN / A733 后端跑通（backend 切换 = 改配置项）
- [ ] M2 慢路径 MCSA + 按工况打标签（P3 正常建模）
- [ ] M3 特征库成型 → NPU 推理 → 过程管理闭环
